{
  "cribs": [
    {
      "id": "C01",
      "title": "if / else / elif",
      "description": "Умовні оператори — дозволяють виконувати код лише коли умова істинна.",
      "extended_description": "<h3>Умовні Оператори ⚠</h3>\n<p>Дозволяють виконувати код лише коли умова істинна.</p>\n\n<h4>🔸 Синтаксис</h4>\n<pre><code>\nif умова:\n    ...\nelif інша_умова:\n    ...\nelse:\n    ...\n</code></pre>\n\n<h4>🔥 Приклад 1</h4>\n<pre><code>\nage = 20\nif age &gt;= 18:\n    print(\"Повнолітній\")\n</code></pre>\n\n<h4>🔥 Приклад 2</h4>\n<pre><code>\nx = 0\nif x &gt; 0:\n    print(\"Плюс\")\nelif x == 10:\n    print(\"Десять\")\nelse:\n    print(\"Нуль або мінус\")\n</code></pre>"
    },
    {
      "id": "C02",
      "title": "while",
      "description": "Цикл, який повторюється, поки умова істинна.",
      "extended_description": "<h3>Цикл while ⚠</h3>\n<p>Повторюється, поки умова істинна.</p>\n\n<h4>🔸 Синтаксис</h4>\n<pre><code>\nwhile умова: # обов'язково умова для зупинки\n    ...\n</code></pre>\n\n<h4>🔥 Приклад 1</h4>\n<pre><code>\ni = 0\nwhile i &lt; 3:\n    print(i)\n    i += 1\n</code></pre>\n\n<h4>🔥 Приклад 2</h4>\n<pre><code>\npassword = \"\"\nwhile password != \"123\":\n    password = input(\"Введіть пароль: \")\n</code></pre>"
    },
    {
      "id": "C03",
      "title": "try / except / finally",
      "description": "Обробка помилок — запобігання \"падінню\" програми.",
      "extended_description": "<h3>Обробка Помилок (try / except) ⚠</h3>\n<p>Забезпечує, щоб програма не завершила роботу при виникненні винятку.</p>\n\n<h4>🔸 Синтаксис</h4>\n<pre><code>\ntry:\n    # Код, який може викликати помилку\n    ...\nexcept &lt;Виняток&gt;:\n    # Обробка конкретної помилки\n    ...\nfinally:\n    # Виконується завжди, незалежно від помилки\n    ...\n</code></pre>\n\n<h4>🔥 Приклад 1</h4>\n<pre><code>\ntry:\n    x = int(\"abc\")\nexcept:\n    print(\"Помилка перетворення\")\n</code></pre>\n\n<h4>🔥 Приклад 2</h4>\n<pre><code>\ntry:\n    f = open(\"file.txt\")\nexcept FileNotFoundError:\n    print(\"Нема файлу\")\nfinally:\n    print(\"Кінець\")\n</code></pre>"
    },
    {
      "id": "C04",
      "title": "break та continue",
      "description": "Керування виконанням циклів.",
      "extended_description": "<h3>Керування Циклами ⚠</h3>\n<ul>\n  <li><strong><code>break</code>:</strong> перериває цикл повністю.</li>\n  <li><strong><code>continue</code>:</strong> переходить до наступної ітерації, пропускаючи решту коду в поточному проході.</li>\n</ul>\n\n<h4>🔥 Приклад break</h4>\n<pre><code>\nfor x in range(10):\n    if x == 5:\n        break\n    print(x) # Виведе 0, 1, 2, 3, 4\n</code></pre>\n\n<h4>🔥 Приклад continue</h4>\n<pre><code>\nfor x in range(10):\n    if x % 2 == 0: # Якщо число парне\n        continue\n    print(x) # Виведе лише непарні числа: 1, 3, 5, 7, 9\n</code></pre>"
    },
    {
      "id": "C05",
      "title": "Логічні оператори (and, or, not)",
      "description": "Комбінування умов. Немає операторів && та ||.",
      "extended_description": "<h3>Логічні Оператори ⚠</h3>\n<p>У Python використовуються ключові слова <code>and</code>, <code>or</code>, <code>not</code>.</p>\n\n<h4>and — логічне «І»</h4>\n<p>Умова істинна, лише якщо всі частини істинні.</p>\n<pre><code>\nage = 20\nif age &gt; 18 and age &lt; 30:\n    print(\"Young adult\")\n\ntemp = 22\nif temp &gt; 15 and temp &lt; 30 and temp != 20:\n    print(\"Комфортно\")\n</code></pre>\n\n<h4>or — логічне «АБО»</h4>\n<p>Умова істинна, якщо хоча б одна частина істинна.</p>\n<pre><code>\ncolor = \"red\"\nif color == \"red\" or color == \"blue\":\n    print(\"Це дозволені кольори\")\n\nrole = \"admin\"\nif role == \"admin\" or role == \"moderator\":\n    print(\"Є доступ\")\n</code></pre>\n\n<h4>not — логічне «НЕ»</h4>\n<p>Інвертує результат умови.</p>\n<pre><code>\nis_online = False\nif not is_online:\n    print(\"Користувач офлайн\")\n\npassword = \"\"\nif not password:\n    print(\"Пароль порожній\")\n</code></pre>"
    },
    {
      "id": "C06",
      "title": "Декоратори: Від Простих до Складних",
      "description": "5 практичних прикладів використання декораторів для логування, вимірювання часу та авторизації.",
      "extended_description": "<h3>Декоратори на Практиці 🛠️</h3>Декоратори дозволяють обгорнути функцію, щоб змінити її поведінку або додати функціональність, не змінюючи її вихідного коду.\n<h4>1. Просте Логування (Logging)</h4>Найпростіший декоратор, який виводить повідомлення перед виконанням функції.\n<pre><code>\ndef log_call(func):\n    def wrapper(*args, **kwargs):\n        print(f\"[LOG] Викликано функцію: {func.__name__}\")\n        return func(*args, **kwargs)\n    return wrapper\n\n@log_call\ndef greet(name):\n    return f\"Привіт, {name}\"\n\nprint(greet('Олег'))\n# Вивід:\n# [LOG] Викликано функцію: greet\n# Привіт, Олег\n</code></pre>\n\n<h4>2. Вимірювання Часу Виконання (@measure_time)</h4>Використовується для профайлингу: вимірювання часу, витраченого на виконання функції.\n<pre><code>\nimport time\n\ndef measure_time(func):\n    def wrapper(*args, **kwargs):\n        start = time.time()\n        result = func(*args, **kwargs)\n        end = time.time()\n        print(f\"Час виконання {func.__name__}: {end - start:.4f} сек\")\n        return result\n    return wrapper\n\n@measure_time\ndef waste_time():\n    time.sleep(0.5)\n\nwaste_time()\n</code></pre>\n\n<h4>3. Перевірка Дозволів (@login_required)</h4>Поширено у веб-фреймворках (Django, Flask). Перевіряє передумови перед виконанням основної функції.\n<pre><code>\ndef login_required(func):\n    def wrapper(user, *args, **kwargs):\n        if user.get('is_authenticated'):\n            return func(user, *args, **kwargs)\n        else:\n            return \"Доступ заборонено. Авторизуйтесь.\"\n    return wrapper\n\n@login_required\ndef get_profile_data(user):\n    return f\"Дані користувача: {user['username']}\"\n\nuser1 = {'username': 'admin', 'is_authenticated': True}\nuser2 = {'username': 'guest', 'is_authenticated': False}\nprint(get_profile_data(user2))\n</code></pre>\n\n<h4>4. Декоратор з Аргументами (Кількість Викликів)</h4>Дозволяє передавати аргументи безпосередньо декоратору (наприклад, скільки разів дозволено викликати функцію).\n<pre><code>\ndef limit_calls(max_calls):\n    def decorator(func):\n        count = 0\n        def wrapper(*args, **kwargs):\n            nonlocal count\n            if count < max_calls:\n                count += 1\n                return func(*args, **kwargs)\n            else:\n                return f\"Виклик {func.__name__} обмежено до {max_calls} разів.\"\n        return wrapper\n    return decorator\n\n@limit_calls(max_calls=2)\ndef api_call():\n    return \"Дані отримано\"\n\nprint(api_call())\nprint(api_call())\nprint(api_call())\n</code></pre>\n\n<h4>5. Кешування Результатів (@functools.lru_cache)</h4>Складний, але дуже корисний приклад. Вбудований декоратор **`lru_cache`** зберігає результати функції у пам'яті (кеші), щоб уникнути повторних обчислень для тих самих вхідних даних.\n<pre><code>\nfrom functools import lru_cache\n\n@lru_cache(maxsize=None)\ndef expensive_calculation(n):\n    print(f\"Обчислення {n}...\")\n    time.sleep(1) \n    return n * 2\n\nexpensive_calculation(5) # Обчислює 1 сек\nexpensive_calculation(5) # Отримує з кешу, миттєво\n</code></pre>"
    },
    {
      "id": "C07",
      "title": "Генератори (yield)",
      "description": "Ліниве повернення значень. Економія пам'яті для великих даних.",
      "extended_description": "<h3>Генератори та yield ⚠</h3>\n<p><strong>Генератор</strong> — це функція, яка поводиться як ітератор, але не повертає всі дані одразу. Вона видає по одному значенню через ключове слово <code>yield</code>, економлячи пам’ять.</p>\n\n<h4>Основна Ідея</h4>\n<p>Замість того, щоб створювати великий список у пам'яті, генератор створює лише наступний елемент, коли його запитують.</p>\n\n<h4>🔥 Приклад 1 — Простий Генератор</h4>\n<pre><code>\ndef gen():\n    yield 1\n    yield 2\n    yield 3\n\nfor x in gen():\n    print(x)\n</code></pre>\n\n<h4>🔥 Приклад 2 — Великий Діапазон (Ефективний)</h4>\n<pre><code>\ndef count_to(n):\n    for i in range(n):\n        yield i\n\n# Цей цикл не займає багато пам'яті, оскільки створюється лише 1 число за раз\n# for x in count_to(1_000_000):\n#     pass\n</code></pre>\n\n<h4>🔥 Приклад 3 — Генератор Файлу</h4>\n<p>Читає файл построчно, не завантажуючи весь вміст у пам’ять, що ідеально для гігантських лог-файлів.</p>\n<pre><code>\ndef read_file(path):\n    with open(path) as f:\n        for line in f:\n            yield line.strip()\n</code></pre>"
    },
    {
      "id": "C08",
      "title": "Генератори: Ліниві Обчислення та `yield`",
      "description": "5 прикладів генераторів, що демонструють економію пам'яті та ліниву ініціалізацію.",
      "extended_description": "<h3>Генератори на Практиці (Ключове слово `yield`) 💾</h3>Генератори — це функції, які повертають ітератор, використовуючи `yield` замість `return`. Вони ідеально підходять для роботи з великими або нескінченними послідовностями, оскільки генерують елементи «на вимогу», економлячи пам'ять. \n<h4>1. Простий Генератор Послідовності (Range)</h4>Аналог вбудованого `range()`, але реалізований як генератор.\n<pre><code>\ndef simple_range(end):\n    i = 0\n    while i < end:\n        yield i\n        i += 1\n\n# Не зберігає весь список у пам'яті, генерує по одному\nfor num in simple_range(5):\n    print(num)\n# Виведе: 0, 1, 2, 3, 4\n</code></pre>\n\n<h4>2. Генератор Квадратів (Generator Expression)</h4>Найпростіший синтаксис (схожий на генератор списку, але з круглими дужками) для лінивого створення послідовності.\n<pre><code>\nnumbers = [1, 2, 3]\n\n# Вираз генератора (круглі дужки)\nsquares_gen = (x * x for x in numbers)\n\nprint(next(squares_gen)) # Виведе: 1\nprint(next(squares_gen)) # Виведе: 4\n</code></pre>\n\n<h4>3. Генератор Читання Великого Файлу</h4>Критично важливий для великих лог-файлів. Замість завантаження всього файлу в пам'ять, він видає рядки по одному.\n<pre><code>\n# Уявімо, що 'large_file.txt' має гігабайти даних.\ndef read_large_file(filename):\n    with open(filename, 'r') as file:\n        for line in file:\n            yield line.strip()\n\n# Ми обробляємо дані рядок за рядком, не переповнюючи пам'ять.\n# for record in read_large_file('log.txt'): ...\n</code></pre>\n\n<h4>4. Генератор Нескінченної Послідовності (Лічильник)</h4>Послідовність, яка теоретично ніколи не закінчиться. Це можливо лише завдяки `yield`, який призупиняє роботу.\n<pre><code>\ndef counter_from(start=0):\n    n = start\n    while True:\n        yield n\n        n += 1\n\ncount = counter_from(10)\nprint(next(count)) # Виведе: 10\nprint(next(count)) # Виведе: 11\n</code></pre>\n\n<h4>5. Генератор Деревоподібного Обходу (Обхід Папок)</h4>Обхід складних рекурсивних структур. Вбудована функція `os.walk` є прикладом такого генератора, який видає вміст папок по мірі їхнього знаходження.\n<pre><code>\nimport os\n\ndef find_py_files(root_dir):\n    for root, dirs, files in os.walk(root_dir):\n        for file in files:\n            if file.endswith('.py'):\n                # Видаємо шлях, як тільки його знаходимо\n                yield os.path.join(root, file)\n\n# for python_file in find_py_files('/home/user/projects'): ...\n</code></pre>"
    },
    {
      "id": "C09",
      "title": "Ітератори: Обхід Об'єктів та Кастомна Логіка",
      "description": "5 прикладів ітераторів, що показують, як створювати власні об'єкти для послідовного доступу.",
      "extended_description": "<h3>Ітератори на Практиці (`__iter__` та `__next__`) ➡️</h3><strong>Ітератор</strong> — це будь-який об'єкт, який реалізує два методи: `__iter__` (повертає сам ітератор) та `__next__` (повертає наступний елемент, або викликає `StopIteration` при закінченні). Це основа циклів `for` у Python. \n<h4>1. Ітератор Стандартного Списку</h4>Найпростіший приклад: отримання ітератора зі стандартного `list` за допомогою вбудованої функції `iter()`.\n<pre><code>\nnames = ['Anna', 'Bob', 'Carl']\n# 1. Створюємо ітератор\nname_iterator = iter(names)\n\n# 2. Викликаємо __next__() вручну\nprint(next(name_iterator)) # Виведе: Anna\nprint(next(name_iterator)) # Виведе: Bob\n# next(name_iterator) для Carl\n# next(name_iterator) вчетверте викличе StopIteration\n</code></pre>\n\n<h4>2. Кастомний Ітератор: Лічильник</h4>Створення класу, який працює як лічильник, що зупиняється на заданому значенні (подібно до `range`).\n<pre><code>\nclass MyCounter:\n    def __init__(self, limit):\n        self.limit = limit\n        self.current = 0\n        \n    def __iter__(self):\n        return self\n\n    def __next__(self):\n        if self.current < self.limit:\n            self.current += 1\n            return self.current - 1\n        else:\n            # Обов'язкова умова для зупинки циклу for\n            raise StopIteration\n\nfor i in MyCounter(3):\n    print(i) # Виведе: 0, 1, 2\n</code></pre>\n\n<h4>3. Ітератор для Кастомного Контейнера (Словник)</h4>Визначення, що має повертати цикл `for`, коли він обходить ваш власний об'єкт-контейнер (наприклад, тільки ключі словника).\n<pre><code>\nclass UserDict:\n    def __init__(self, data):\n        self.data = data\n\n    def __iter__(self):\n        # Повертаємо ітератор ключів словника\n        return iter(self.data.keys())\n\nusers = UserDict({'id_1': 'Alice', 'id_2': 'Bob'})\nfor user_id in users:\n    print(user_id) \n# Виведе: id_1, id_2\n</code></pre>\n\n<h4>4. Ітератор, що Повторює Елементи (Cycle)</h4>Складніший приклад, де ітератор скидається до початку після досягнення кінця (подібно до `itertools.cycle`).\n<pre><code>\nfrom itertools import cycle\n\ncolors = cycle(['red', 'green', 'blue'])\nprint(next(colors)) # red\nprint(next(colors)) # green\n# ... після blue знову поверне red\n</code></pre>\n\n<h4>5. Ітератор, Створений за Допомогою `zip()`</h4>Вбудовані функції, такі як `zip()`, `map()` та `filter()`, повертають **ітератори**. Це допомагає уникнути створення проміжних списків у пам'яті.\n<pre><code>\nnames = ['A', 'B', 'C']\nscores = [90, 85, 92]\n\n# zip() повертає ітератор\npairs_iterator = zip(names, scores)\n\n# Обробляємо елементи по одному\nfor name, score in pairs_iterator:\n    print(f'{name}: {score}')\n</code></pre>"
    },
    {
      "id": "C10",
      "title": "Модулі (import)",
      "description": "Підключення коду з інших файлів (.py) або вбудованих бібліотек.",
      "extended_description": "<h3>Модулі та Імпорт ⚠</h3>\n<p><strong>Модуль</strong> — це будь-який файл <code>.py</code>, який містить функції, класи та змінні. Імпорт дозволяє використовувати цей код в іншому місці.</p>\n\n<h4>Типи Імпорту</h4>\n<ol>\n  <li><strong>Повний імпорт:</strong> <code>import &lt;модуль&gt;</code></li>\n  <li><strong>Частковий імпорт:</strong> <code>from &lt;модуль&gt; import &lt;функція&gt;</code></li>\n</ol>\n\n<h4>🔥 Приклад 1 — Частковий Імпорт</h4>\n<pre><code>\nfrom random import randint\n\n# Викликається функція без префікса модуля\nprint(randint(1, 10))\n</code></pre>\n\n<h4>🔥 Приклад 2 — Повний Імпорт</h4>\n<pre><code>\nimport math\n\n# Викликається з префіксом модуля\nprint(math.sqrt(16))\n</code></pre>"
    },
    {
      "id": "C11",
      "title": "*args / **kwargs",
      "description": "Прийом невідомої кількості позиційних (*args) або іменованих (**kwargs) аргументів у функції.",
      "extended_description": "<h3>Змінна Кількість Аргументів ⚠</h3>\n<p>Використовується, коли кількість аргументів, які буде передано у функцію, невідома заздалегідь.</p>\n\n<h4>*args (Позиційні аргументи)</h4>\n<p>Збирає всі позиційні аргументи у <strong>кортеж</strong>.</p>\n<pre><code>\ndef add(*nums):\n    return sum(nums)\n\nprint(add(1, 2, 3)) # nums = (1, 2, 3)\n# Вивід: 6\n</code></pre>\n\n<h4>**kwargs (Іменовані аргументи)</h4>\n<p>Збирає всі іменовані аргументи (ключ=значення) у <strong>словник</strong>.</p>\n<pre><code>\ndef info(**data):\n    print(data)\n\ninfo(name=\"Alex\", age=25) # data = {'name': 'Alex', 'age': 25}\n</code></pre>"
    },
    {
      "id": "C12",
      "title": "Comprehensions (List/Dict/Set)",
      "description": "Короткий, швидкий і \"пітонічний\" спосіб створення колекцій.",
      "extended_description": "<h3>Comprehensions (Стисле Створення Колекцій) ⚠</h3>\n<p>Це компактний спосіб створення списків, словників та множин, часто швидший і читабельніший за класичний цикл <code>for</code>.</p>\n\n<h4>🔸 Синтаксис</h4>\n<pre><code>\n[вираз for елемент in iterable if condition]\n</code></pre>\n\n<h4>List Comprehension (Створення Списку)</h4>\n<pre><code>\n# Квадрати чисел\nnums = [x*x for x in range(5)] # [0, 1, 4, 9, 16]\n\n# Фільтр парних\neven = [x for x in range(10) if x % 2 == 0] # [0, 2, 4, 6, 8]\n\n# Перетворення рядків\nnames = [\" Tom \", \" Ana \", \" Bob \"]\ncleaned = [n.strip().upper() for n in names] # ['TOM', 'ANA', 'BOB']\n</code></pre>\n\n<h4>Dict Comprehension (Створення Словника)</h4>\n<pre><code>\n# Число -&gt; квадрат\nd = {x: x*x for x in range(5)} # {0: 0, 1: 1, 2: 4, 3: 9, 4: 16}\n\n# Інвертування словника\noriginal = {\"a\": 1, \"b\": 2}\ninverted = {v: k for k, v in original.items()} # {1: 'a', 2: 'b'}\n</code></pre>\n\n<h4>Set Comprehension (Створення Множини)</h4>\n<pre><code>\n# Унікальні символи\nunique_chars = {c for c in \"hello world\"} # {'h', 'e', 'l', 'o', ' ', 'w', 'r', 'd'}\n\n# Видалення повторів із списку\nnumbers = [1,2,2,3,3,3,4]\nunique = {n for n in numbers} # {1, 2, 3, 4}\n</code></pre>"
    },
    {
      "id": "C13",
      "title": "staticmethod / classmethod",
      "description": "Спеціальні методи всередині класу для роботи без об'єкта (@staticmethod) або з класом (@classmethod).",
      "extended_description": "<h3>Декоратори Методів Класу ⚠</h3>\n<p>Використовуються для визначення методів, які не вимагають об'єкта (instance) або працюють з властивостями самого класу.</p>\n\n<h4>@staticmethod</h4>\n<ul>\n  <li><strong>Принцип:</strong> Не отримує ані <code>self</code> (об'єкт), ані <code>cls</code> (клас).</li>\n  <li><strong>Призначення:</strong> Звичайна функція, логічно пов'язана з класом, але не використовує його властивості.</li>\n</ul>\n<pre><code>\nclass Math:\n    @staticmethod\n    def add(a, b):\n        return a + b\n\nprint(Math.add(2, 3))\n</code></pre>\n\n<h4>@classmethod</h4>\n<ul>\n  <li><strong>Принцип:</strong> Отримує посилання на сам клас (<code>cls</code>) як перший аргумент.</li>\n  <li><strong>Призначення:</strong> Фабричні методи, альтернативні конструктори, робота з властивостями класу.</li>\n</ul>\n<pre><code>\nclass User:\n    species = \"human\"\n\n    @classmethod\n    def from_email(cls, email):\n        # Використовуємо cls для створення об'єкта\n        name = email.split(\"@\")[0]\n        return cls(name)\n\n# Створюємо об'єкт через альтернативний конструктор\nu = User.from_email(\"alex@gmail.com\")\n</code></pre>\n\n<h4>🔥Порівняння</h4>\n<table>\n  <tr>\n    <th>Особливість</th>\n    <th>@staticmethod</th>\n    <th>@classmethod</th>\n    <th>Звичайний метод</th>\n  </tr>\n  <tr>\n    <td>Отримує <code>self</code></td>\n    <td>❌</td>\n    <td>❌</td>\n    <td>✅</td>\n  </tr>\n  <tr>\n    <td>Отримує <code>cls</code></td>\n    <td>❌</td>\n    <td>✅</td>\n    <td>❌</td>\n  </tr>\n  <tr>\n    <td>працює з класом </td>\n    <td>❌</td>\n    <td>✅</td>\n    <td>❌</td>\n  </tr>\n  <tr>\n    <td>працює з обʼєктом </td>\n    <td>✅</td>\n    <td>✅</td>\n    <td>✅</td>\n  </tr>\n      </table>"
    },
    {
      "id": "C14",
      "title": "Type Hints (анотації типів)",
      "description": "Рекомендації щодо типів для кращої читабельності та роботи IDE/лінтерів.",
      "extended_description": "<h3>Type Hints (Анотації Типів) ⚠</h3>\n<p>Це синтаксичні підказки, які не впливають на виконання коду (Python залишається динамічно типізованим), але допомагають IDE, лінтерам та іншим розробникам зрозуміти, які типи даних очікуються.</p>\n\n<h4>🔥 Приклад 1 — Аргументи та Повернення</h4>\n<pre><code>\ndef add(a: int, b: int) -&gt; int:\n    return a + b\n\n# -&gt; int: вказує тип поверненого значення\n</code></pre>\n\n<h4>🔥 Приклад 2 — Складні Типи (Lists, Dicts)</h4>\n<pre><code>\nfrom typing import List\n\ndef total(nums: List[int]) -&gt; int:\n    # nums очікується як Список, що містить цілі числа\n    return sum(nums)\n</code></pre>"
    },
    {
    "id": "C15",
    "title": "ООП: Синтаксис Класу, Успадкування 🧱",
    "description": "Збірник прикладів правильного синтаксису для класів, успадкування, множин та значення None.",
    "extended_description": "<h3>🔸 Синтаксис Класу та Шаблон для Об'єктів</h3>\n<p>Клас — це креслення, що визначає атрибути (дані) та методи (дії), які матимуть усі його об'єкти (екземпляри).</p>\n<pre><code>\nclass User:\n    # Атрибут класу (спільний)\n    role = \"guest\" \n\n    # Конструктор (ініціалізація об'єкта)\n    def __init__(self, name, age):\n        self.name = name  # Атрибут екземпляра\n        self.age = age\n    \n    # Метод\n    def greet(self):\n        # self посилається на поточний об'єкт\n        print(f\"Hello, my name is {self.name}\")\n        \n# Клас як шаблон: створення об'єктів\nuser1 = User(\"Oleh\", 30)\nuser2 = User(\"Anna\", 25)\n\nuser1.greet() \n# Виведе: Hello, my name is Oleh\n</code></pre>\n\n<h3>🔸 Успадкування (Inheritance)</h3>\n<p>Дочірній клас (підклас) наслідує атрибути та методи від Батьківського класу (суперкласу).</p>\n<pre><code>\nclass Animal: # Батьківський клас\n    def move(self):\n        print(\"Moving...\")\n\nclass Dog(Animal): # Дочірній клас\n    def move(self):\n        # Виклик методу батька для розширення\n        super().move() \n        print(\"Running...\")\n\ndog = Dog()\ndog.move()\n# Виведе:\n# Moving...\n# Running...\n</code></pre>\n\n"
    },
    {
    "id": "16",
    "title": "ООП, Поліморфізм та Інкапсуляція 🧱",
    "description": "Збірник прикладів правильного синтаксису для класів, успадкування, множин та значення None.",
    "extended_description": "<h4>🔸 Поліморфізм та Інкапсуляція</h4>\n<p><strong>Поліморфізм</strong>: Різні об'єкти мають однаковий метод, але з різною реалізацією.<br><strong>Інкапсуляція</strong>: Приховування внутрішніх даних об'єкта (за допомогою префіксів).</p>\n<pre><code>\n# --- Поліморфізм ---\nclass Cat:\n    def sound(self): return \"Meow\"\n        \nclass Duck:\n    def sound(self): return \"Quack\"\n\nfor creature in [Cat(), Duck()]:\n    print(creature.sound()) \n# Виведе: Meow, Quack\n\n# --- Інкапсуляція ---\nclass BankAccount:\n    def __init__(self, balance):\n        # Приватний атрибут (__balance) за домовленістю\n        self.__balance = balance\n        \n    def get_balance(self): # Публічний геттер\n        return self.__balance\n\nacc = BankAccount(100)\nprint(acc.get_balance()) # Виведе: 100\n</code></pre>"
    },
    {
    "id": "C17",
    "title": "Множини та NoneType 🧩",
    "description": "Збірник прикладів правильного синтаксису для класів, успадкування, множин та значення None.",
    "extended_description": "<h4>🔸 Множина: `set` та `frozenset` 🧩</h4>\n<p>Множина (`set`) — колекція унікальних елементів. `frozenset` — незмінна множина, яку можна використовувати як ключ словника.</p>\n<pre><code>\n# --- set: Змінювана множина ---\nmy_set = {1, 2, 3, 1, 4}\nmy_set.add(5)\nprint(my_set) # Виведе: {1, 2, 3, 4, 5}\n\n# Операції над множинами\nA = {1, 2, 3}\nB = {3, 4, 5}\nintersection = A & B  # Перетин\ndifference = A - B    # Різниця\nprint(f\"Перетин: {intersection}\") # Виведе: Перетин: {3}\n\n# --- frozenset: Незмінна множина ---\nfrozen = frozenset([1, 2])\ndic = {frozen: \"Value\"} # Використовуємо як ключ\nprint(dic[frozen]) # Виведе: Value\n</code></pre>\n\n<h4>🔸 Тип NoneType (Значення None)</h4>\n<p><strong>None</strong> позначає відсутність значення. Це єдиний об'єкт типу `NoneType`.</p>\n<pre><code>\n# Функція, що повертає None\ndef get_user(is_logged_in):\n    if is_logged_in:\n        return \"Admin\"\n    # інакше повертається None\n    \nuser = get_user(False) \nprint(user) # Виведе: None\n\n# Рекомендована перевірка на None\nif user is None:\n    print(\"User is not defined\")\n# Виведе: User is not defined\n\n# None у булевому контексті\nif not user: \n    print(\"Умова істинна\") # Виведе: Умова істинна\n</code></pre>"
    }
  ]
}
